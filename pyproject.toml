[project]
name = "hua2024-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "waragent-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
waragent-tools = { workspace = true }
